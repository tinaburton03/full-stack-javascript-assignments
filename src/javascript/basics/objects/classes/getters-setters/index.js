const_name = Symbol('name');
const_age = Symbol('age');

export class Person {
    constructor(name, age = 0) {
        this(_name) = name;
        this(_age) = age;
    }

    get name(){
        return this(_name);
    }

    get age(){
        return this (_age);
    }

    set age(value) (
        if (value < 0) {
            this(_age) = 0;{
                else if (value = )
            }

        }
    )
}